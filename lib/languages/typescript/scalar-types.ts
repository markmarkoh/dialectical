// Numbers (both integers and floats use the same type)
const age = 30
const height = 1.75
const sum = age + 5 // 35
const area = height * 2.0 // 3.5

// Booleans
const isActive = true
const isValid = isActive && age > 18

// Strings
const name = "Alice"
const greeting = "Hello, " + name // String concatenation

// Template literals (string interpolation)
const message = `Name: ${name}, Age: ${age}`

// BigInt for large integers
const bigNumber = 9007199254740991n

