/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Ensure the examples directory is included in the build
  output: 'standalone',
}

export default nextConfig

